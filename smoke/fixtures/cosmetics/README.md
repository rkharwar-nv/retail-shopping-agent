# cosmetics fixtures

Drop `.jpg`/`.jpeg`/`.png`/`.webp` images here. Each image becomes
a smoke fixture classified as `cosmetics`.

Optional sidecar `.txt` files override the default prompt.
See `smoke/README.md` for details.
